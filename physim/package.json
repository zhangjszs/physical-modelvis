{
  "name": "physim",
  "version": "1.0.0",
  "description": "Electromagnetic field & charged particle physics engine for the web - Boris integrator, RK45, analytic fields",
  "main": "dist/physim.js",
  "module": "dist/physim.esm.js",
  "types": "dist/physim.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --format=esm --outfile=dist/physim.esm.js && npx esbuild src/index.ts --bundle --format=iife --global-name=PhysSim --outfile=dist/physim.js",
    "test": "npx tsx test/physics.test.ts"
  },
  "keywords": ["physics", "electromagnetic", "boris", "charged-particle", "simulation", "lorentz-force"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "tsx": "^4.7.0"
  }
}
