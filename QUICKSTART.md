# 快速启动指南

## 前置要求

- Node.js (>= 18.0)
- 现代浏览器（Chrome/Edge/Firefox，支持 WebGL）

## 安装步骤

### 1. 安装 Node.js（如果尚未安装）

```bash
# Windows (使用 winget)
winget install OpenJS.NodeJS

# 或者从官网下载
# https://nodejs.org/
```

### 2. 构建物理引擎

```bash
cd physim
npm install
npm run build
```

成功后会生成 `physim/dist/physim.js` 和 `physim/dist/physim.esm.js`。

### 3. 启动本地服务器

```bash
# 在项目根目录执行
npx http-server . -p 8080
```

或者直接双击打开 `index.html`（部分浏览器可能需要禁用 CORS）。

### 4. 访问系统

浏览器打开: `http://localhost:8080`

## 使用教程

### 基本操作

1. **加载题目**: 点击顶部标签切换不同题目
2. **播放仿真**: 点击右侧"播放"按钮或选择发射模式
3. **调节速度**: 拖动速度滑块（0.1x - 3.0x）
4. **视角控制**:
   - 左键拖拽：旋转视角
   - 右键拖拽：平移视角
   - 滚轮：缩放

### 发射模式

| 模式 | 说明 | 适用场景 |
|------|------|---------|
| 单粒子 | 发射单个粒子 | 观察单个轨迹 |
| 多角度 | 同时发射多个粒子（36个方向） | 验证击中率 |
| 连续 | 持续发射粒子 | 观察粒子束 |
| 验证选项 | 根据选项自动演示 | 答题验证 |

### 显示控制

- ☑ 轨迹：显示/隐藏粒子运动轨迹
- ☑ 击中点：显示/隐藏粒子击中位置标记
- ☑ 场符号：显示/隐藏电场箭头和磁场×符号

### 查看推导

1. 点击左侧选项（A/B/C/D）
2. 底部推导面板会显示对应选项的计算过程
3. 3D 场景中会自动添加标注（如半径、区域长度等）

### 查看答案

点击右上角"答案"按钮，正确答案会高亮显示（绿色为正确，红色为错误）。

## 示例：白银三模题目

### 题目描述

平行板间存在匀强磁场，中心有粒子源向各个方向发射电子。已知板间距为 4d，磁感应强度 B = mv₀/(2ed)，求：

A. 轨迹半径 R = 2d  
B. 周期 T = πd/v₀  
C. 击中区域总长度 L = 4(√3+1)d  
D. 击中率为 50%

### 验证步骤

1. **加载题目**: 点击顶部"带电粒子在磁场中的运动"标签
2. **验证选项 A**:
   - 点击左侧选项 A
   - 观察推导面板中的公式推导
   - 场景中会标注 R=2d 的黄色虚线
3. **验证选项 C**:
   - 点击选项 C
   - 观察左右极板上的红色/蓝色高亮区域
   - 查看标注的最高点和最低点坐标
4. **验证选项 D**:
   - 选择"多角度"发射模式
   - 观察所有粒子都打到极板上
   - 信息面板显示"击中数 = 发射数"
5. **结论**: 正确答案为 A、C

## 常见问题

### Q: 页面显示空白/加载失败

**A**: 检查以下几点：
1. 确认 `physim/dist/physim.js` 文件存在（需要先构建）
2. 使用本地服务器而非直接打开 HTML 文件
3. 浏览器控制台是否有报错信息

### Q: 粒子不运动

**A**: 
1. 确保已点击"播放"按钮
2. 检查是否选择了发射模式（单粒子/多角度）
3. 尝试点击"重置"后重新播放

### Q: 3D 视角卡顿

**A**:
1. 减少同时显示的粒子数量
2. 降低轨迹长度（修改代码中的 `trailLength` 参数）
3. 关闭轨迹显示（取消勾选"轨迹"复选框）

### Q: 如何添加自定义题目？

**A**: 
1. 在 `problems/` 目录下创建新文件
2. 参考 `baiyin-sanmo.js` 的格式定义问题配置
3. 在 `index.html` 第 740 行附近注册新问题：
   ```javascript
   PhysVis.ProblemRegistry.register(MyNewProblem);
   ```
4. 如需新场景模板，在 `templates/scene-templates.js` 中添加

### Q: 物理计算不准确？

**A**: 
1. 运行测试套件验证：`cd physim && npm test`
2. 检查积分器类型是否正确（纯磁场推荐 `analytic_circular`，复合场用 `boris`）
3. 调整时间步长 `dt`（减小可提高精度但会降低性能）

## 开发调试

### 启用调试模式

在浏览器控制台输入：

```javascript
// 查看当前仿真状态
console.log(PhysVis.SimulationManager.getSimulationState(App.currentProblemId));

// 手动发射粒子
Simulator.emitSingle(Math.PI / 4); // 45度角发射

// 查看粒子数据
console.log(Simulator.particles);
```

### 修改物理参数

编辑 `problems/*.js` 文件中的 `given` 对象：

```javascript
given: {
    d: 0.01,              // 改变距离参数
    magneticField: 0.5,   // 改变磁场强度
    initialVelocity: 1e6  // 改变初速度
}
```

刷新页面即可看到效果。

## 下一步

- 阅读 [SYSTEM_ARCHITECTURE.md](SYSTEM_ARCHITECTURE.md) 了解系统架构
- 查看 `physim/src/` 源码学习物理引擎实现
- 尝试创建自己的场景模板和问题配置

## 技术支持

如遇问题，请检查：
1. GitHub Issues（如有）
2. 浏览器控制台错误信息
3. Node.js 版本是否符合要求

祝学习愉快！
